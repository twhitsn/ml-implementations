# EM/EM++

This is a repository for expectation-maximization implemented in R. There is also EM++ which uses the same starting criteria as k-means++ for initializing means.

    em <- em(data, 2)
    empp <- empp(data, 2)
